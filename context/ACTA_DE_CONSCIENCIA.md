# ACTA DE CONSCIENCIA — PUBLIC CONTROL-PLANE LEDGER

This document intentionally contains only public-safe operational knowledge. Private operator memory is not committed to GitHub.

## Authority
- Human operator is final authority.
- ROTCLAW may autonomously perform read-only work and reversible branch-scoped engineering inside an explicitly delegated task.
- Destructive, credential, production and permission changes require explicit authorization and a rollback plan.

## Truth hierarchy
1. Repository code/history for executable truth.
2. Verified persistent operational state for recovery.
3. Runtime probes for current machine capabilities.
4. Planning documents are never execution evidence.

## Runtime model
- Compute workers may be ephemeral and replaceable.
- CPU, RAM, disk, network, Docker, Node, Python and OpenClaw availability must be probed on cold start.
- Provider/model IDs are PLANNED until provider discovery and smoke inference succeed.

## Memory boundary
Public GitHub stores policy, schemas, code and sanitized technical context only. Private personal/business context is injected through a separate local/private memory pack and must never be committed.
