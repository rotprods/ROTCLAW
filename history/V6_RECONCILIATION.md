# v1 → v6 reconciliation

v6 is a `FULL_COMPOSITE` reconstruction, not a linear copy of v5.

## Restored from v1
- install/bootstrap/live verification scripts
- threat model
- workspace identity and tool governance
- policy/config historical references
- original skills: define-goal, gauntlet-loop, graph-engineering, model-router, vibecarrusel

## Retained from later lineage
- v3/v4 graph, epistemics, recovery and QA evidence
- v5 router, provider template, CI and public-safe control plane

## Promotion rule
Historical v1 config/policy are references until validated against the exact current OpenClaw runtime. v6 becomes canonical only after ancestry, secret, schema, QA, cold-restore and replication gates pass. Runtime/provider claims require separate live qualification.
